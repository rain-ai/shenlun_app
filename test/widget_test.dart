import 'package:flutter_test/flutter_test.dart';

import 'package:shenlun_app/main.dart';

void main() {
  test('ShenlunApp can be constructed', () {
    const app = ShenlunApp();

    expect(app, isA<ShenlunApp>());
  });
}
